
<a href="#" id="next">Next</a>


{{#each data}}
<div class="news-item">
  <h1>{{name}}</h1>
  <img src="{{image}}" />
  <p>{{description}}
  <a href="{{link}}">{{link}}</a>
  <div class="detail">
  {{#each source}}
    <div class="detail-item">
      {{{description}}}
    </div>
  {{/each}}
  </div>
</div>
{{/each}}
